# Pilates Booking System

This project is a simple web-based booking system for Pilates classes.  
It uses PHP sessions to store user data across multiple pages and will later  
connect to a MySQL database to save bookings.

## Features (so far)
- Multi-page form flow
- Session-based data storage
- Confirmation page
